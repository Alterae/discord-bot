import Command from "../../types/command";
import flip from "./flip";

/**
 * The random module.  Contains commands for generating random outputs.
 */
const commands: Command[] = [flip];

export default commands;
